package darkoverload.itzip.feature.resume.service;


import darkoverload.itzip.feature.resume.repository.resume.ResumeJpaRepository;
import darkoverload.itzip.feature.resume.service.resume.ResumeService;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
public class ResumeServiceTest {

    @InjectMocks
    private ResumeService resumeService;

    @Mock
    private ResumeJpaRepository resumeJpaRepository;

    @Test
    void 이력서_조회_테스트(){

        //given
//        resumeService.search(request);
2

        // when



        // then
    }


}
